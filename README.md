# Toolia Landing

Landing page statica per `toolia.voler.ai`.

Struttura:
- `public/landing.html` — landing HTML
- `public/landing/` — asset (immagini, App embed)
- `next.config.ts` — rewrite `/` → `/landing.html`

Deploy: Railway auto-detect Next.js. Nessuna env var richiesta.
